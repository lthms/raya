# Flux's own configuration, which is the only thing this directory holds.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - cluster.yaml
  - kube-system.yaml
  - cnpg-system.yaml
  - postgresql.yaml
  - cloud-lab.yaml

