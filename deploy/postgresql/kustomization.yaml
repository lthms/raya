---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # The server, its backup destination and the nightly base backup.
  - cluster.yaml
  - objectstore.yaml
  - r2-secret.yaml
  - scheduledbackup.yaml
  # The operator carving databases out of it.
  - helmrepository.yaml
  - helmrelease.yaml
  - secret.yaml

