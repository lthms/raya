apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-vars
  namespace: flux-system
data:
  hello_hostname: "{{ hello_hostname }}"
  oidc_hostname: "{{ oidc_hostname }}"
  control_plane_private_ip: "{{ private_ip }}"
  acme_email: "{{ acme_email }}"
  gcp_project: "{{ gcp_project }}"
  wif_audience: "{{ wif_audience }}"
